"use client";
import { useState } from "react";
import Sidebar from "@/components/shared/Sidebar";
import Navbar from "@/components/shared/Navbar";
import { FiX } from "react-icons/fi";
import "./globals.css";

const metadata = {
  title: "EchoGPT-AI Chatbox",
  description: "Generated by EchoGPT App",
};

export default function RootLayout({ children }) {
  const [isSidebarOpen, setIsSidebarOpen] = useState(false);

  const toggleSidebar = () => {
    setIsSidebarOpen(!isSidebarOpen);
  };

  return (
    <html lang="en">
      <body>
        <div className="flex h-screen w-full">
          {/* Sidebar - Full width on mobile, no extra space */}
          <div
            className={`fixed inset-0 z-20 bg-[#F7F5FF] transition-transform transform md:relative md:translate-x-0 md:w-auto ${
              isSidebarOpen ? "translate-x-0" : "-translate-x-full"
            } md:block flex flex-col items-center justify-baseline w-full`}
          >
            <div className="absolute top-4 right-4 md:hidden">
              <button
                onClick={toggleSidebar}
                className="text-2xl text-gray-600"
              >
                <FiX />
              </button>
            </div>
            <Sidebar />
          </div>

          {/* Main Content */}
          <div className="flex-1 flex flex-col">
            <Navbar
              toggleSidebar={toggleSidebar}
              isSidebarOpen={isSidebarOpen}
            />
            <main className="w-full">{children}</main>{" "}
            {/* Ensures full width */}
          </div>
        </div>
      </body>
    </html>
  );
}
